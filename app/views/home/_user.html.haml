.span-13.match[user]{'data-id' => user.id, 'data-handle' => user.handle}
  .span-2#picture
    = image_tag(user.primary_photo_url, :size => '50x50', :alt => "#{user.handle}", 'data-handle' => user.handle, 'data-city' => user.city.try(:name), 'data-gender' => user.gender_name.try(:titleize), 'data-matchie' => I18n.t("user.matchie.#{user.matchie.to_s}"))
  .span-4{:style => 'font-size: 0.9em;'}
    #user_handle= user.handle
    #user_meta= user_profile_blurb(user)
    #user_matchie= I18n.t("user.matchie.#{user.matchie.to_s}")
  .span-4{:style => 'font-size: 0.9em;'}
    #user_tag_badges= user.tag_badges_list.sort.join(', ')
  .span-3.last.hide#user_toggle
    = link_to I18n.t("home.stream.meet_user", :name => user.singular_object), '#', :class => 'admin', :id => 'meet_user'
