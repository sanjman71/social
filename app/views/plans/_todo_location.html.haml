- location = locationship.location

.span-8.prepend-1
  %span.name= location.name
  %br
  %span.street= location.street_city
.span-12.last.planned_at
  Added: #{locationship.todo_at.try(:today?) ? 'Today' : locationship.todo_at.try(:to_s, :datetime_short)}
  %br
  %strong #{pluralize(locationship.todo_days_left, 'day')} left to check in here