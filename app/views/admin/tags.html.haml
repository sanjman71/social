- javascript("highcharts")
- javascript('admin_charts')
- javascript('admin_tags')
- stylesheet('admin')

#page-header{:style => 'background: #fff;'}
  .span-24.last
    = render(:partial => 'admin_links')

#content.home-content.clearfix
  #container{:style => 'width: 900px; height: 500px;'}

:javascript
  var tag_names     = #{@tag_names};
  var tag_counts    = #{@tag_counts};
  var badge_counts  = #{@badge_counts};
